/**
 * Wind in grid-index space (must stay in sync with building / sediment logic).
 * [1, 0] → air moves toward +i, which maps to +X in `TileMap` world layout.
 */
export const WIND_DIR_I = 1;
export const WIND_DIR_J = 0;
